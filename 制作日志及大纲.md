# nook 制作日志及大纲

> 最后更新：2026-08-20  
> 本文用于项目交接。更换 AI、账号或电脑后，先阅读本文，再查看 `README`、`package.json`、`supabase/migrations/` 和近期 Git 提交。本文不记录任何密钥、密码或 Supabase Key。

## 1. 项目定位

**nook** 是一个个人博客与公益免费资源分享网站。

- 发布博客、教程、学习笔记、工具介绍与项目记录。
- 收录免费工具、开源项目、学习/设计/开发资源、模板、字体、图片、API 与公共服务。
- 不做付费会员、订阅收费、Stripe、AI 功能、广告核心业务、商城或复杂社区。
- 内容优先，长期可维护；视觉要现代、克制、有质感。

## 2. 已确定的产品与视觉决策

- 正式名称：**nook**。
- 当前 Logo：先保留现有 **B** 图标；后续可替换为正式 nook 标志。
- 默认内容语言：中文；界面支持中文 / 英文切换。第一版不要求每篇内容都维护双语版本。
- 桌面端目标结构：参考用户提供的知识站截图，采用 **左侧固定导航 + 中间主内容 + 右侧信息栏**。
- 右侧栏优先放资源分类、热门标签、最近更新；不照搬订阅、留言板、复杂互动。
- 图片风格：后续可使用克制的书桌、自然、阅读、工作流图片；必须确保免费授权或自制。当前不上传正式品牌图标。
- 深色模式：保留，使用认真设计的深灰 / 深蓝灰，不是简单纯黑反色。
- 动画：仅短促微交互（卡片、按钮、菜单、筛选），尊重 `prefers-reduced-motion`；禁止重型 3D、粒子、持续背景与夸张滚动动画。

## 3. 技术架构

| 项目 | 当前选择 |
|---|---|
| 框架 | Next.js 16 + React 19 + TypeScript |
| 样式 | Tailwind CSS + 现有 UI 组件 |
| 数据 / 登录 | Supabase（PostgreSQL、Auth、Storage）|
| 部署 | 尚未决定；目标用户主要在中国，部署策略留到上线前讨论 |
| 代码仓库 | `https://github.com/fuuuggh/git` |
| 工作分支 | 本地 `main` 跟踪远端 `mvp-foundation`；推送目标为 `origin/mvp-foundation`，不要强推或覆盖远端 `main` |

## 4. 已完成内容

### 4.1 内容与资源功能

- 前台：首页、博客列表 `/blog`、文章详情 `/blog/[slug]`、资源库 `/resources`、资源详情 `/resources/[slug]`、投稿 `/submit`、搜索、关于、联系、条款、隐私政策。
- 后台：审核队列 `/admin/reviews`、文章管理 `/admin/posts`、发布文章 `/admin/posts/new`、资源管理 `/admin/resources`、发布资源 `/admin/resources/new`。
- 资源支持官网、GitHub、外部下载地址、资源类型、平台、价格、开源状态、标签、分类、最后检查时间、失效反馈。
- 管理员可发布/下架文章，发布资源，审核资源投稿，处理失效反馈。
- 公开资源支持本站托管附件与外部下载链接。

### 4.2 公开投稿与附件安全流程

普通访客可提交多种资源：官网、GitHub、下载链接，或单个待审核附件。

1. 附件进入 **私有** `submission-files` Storage bucket，不会直接公开。
2. 管理员在审核页通过受权限保护的临时链接查看附件。
3. 审核通过时，附件从私有 bucket 复制到公开 `resource-files`，并创建资源与附件记录。
4. 审核拒绝时，会删除私有待审附件。

当前限制：投稿附件 10 MB；管理员上传的公开附件 25 MB。允许 PDF、ZIP、TXT、JPG、PNG、WebP 等安全白名单类型。

> 上线前仍建议增加 CAPTCHA（例如 Turnstile）或更可靠的服务端限流，防止匿名附件接口被滥用。

### 4.3 数据库迁移

已由项目 Owner 在 Supabase SQL Editor 执行：

- `supabase/migrations/202608190001_initial_public_site.sql`
- `supabase/migrations/202608190002_resource_attachments.sql`
- `supabase/migrations/202608190003_public_resource_submissions.sql`

- `supabase/migrations/202608190004_post_images.sql`（已于 2026-08-19 执行，创建 `post-images` 存储桶与管理员上传权限）

不要重复运行未审查的迁移。若新环境迁移缺失，先检查对应表、Storage buckets、RLS policies 再执行。

### 4.4 双语与 SEO

- 中英界面切换以 `site_locale` Cookie 保存，默认中文。
- 主要前台和当前后台入口已接入双语。
- 已有 Sitemap、RSS、robots、基础 Metadata、资源与博客的公开路径。
- 已清理当前可见导航中的旧商业/订阅导向入口；旧模板遗留路由仍在仓库内，但不作为产品主路径。

### 4.5 性能与本地开发

- 使用 `next dev --webpack`，因为 Turbopack 在此机器上曾发生内存问题。
- 开发服务运行在 `localhost:3000`，会持续占用本机 CPU、内存和电量；不需要预览时必须停止。
- 停止方式：在启动服务的终端按 `Control + C`。
- 已确认：关闭浏览器不一定会停止开发服务器；以端口/进程是否存在为准。
- 常规验证使用 `npm run typecheck`、`npm run lint`、`npm run build`，不应长期挂着开发服务。

## 5. 当前 Git 状态与关键提交

截至本文更新，工作树已推送到 `mvp-foundation`。近期关键提交：

- `0785b08`：建立公益博客与资源 MVP 基础。
- `2c95f4a`、`dd4af2f`：降低本地/后台页面等待远端 Auth 的影响。
- `0aa4511`：资源审核与状态管理。
- `7b61d69`：文章管理与投稿发布。
- `9e28771`、`81efe0a`：Sitemap 与博客公开内容路径。
- `08cac69`：中文/英文界面基础。
- `b64a898`：统一后台导航、扩展资源发布。
- `6038cd2`：公开内容元数据与下载支持。
- `b410ff0`、`425f90b`：多类型公开投稿与拒绝附件清理。
- `e61f5e6`：整理旧后台入口与双语遗留文本。
- `3d5bb6e`：管理员查看私有投稿附件。
- `3776e33`：本地化 404 页面。

## 6. 下一阶段路线图

### Phase A：内容编辑与媒体（下一步）

1. **已完成并已运行迁移**：后台新建文章支持 1 张封面与最多 8 张正文图片。支持 JPG、PNG、WebP、AVIF；单张 6 MB，合计 20 MB。正文图片按上传顺序附在正文后。
2. 补齐文章编辑、资源编辑、分类与标签管理，避免只能新建不能维护。
3. 优化审核页的错误提示、附件预览与资源状态处理。

### Phase B：nook 视觉系统

1. **已完成基础框架**：站点名称改为 nook，保留 B 作为临时图标；SEO 基础名称同步更新。
2. **已完成基础框架**：建立暖白 / 墨黑 / 深蓝灰深色模式色板，统一圆角与边框基调。
3. **已完成基础框架**：桌面端改为左侧固定导航 + 中心内容 + 右侧信息栏；移动端保留单栏和抽屉菜单。
4. 进行中：首页 Hero、主题入口、最新文章卡片和精选资源区已重做；桌面顶部搜索、博客封面卡片、资源卡片和资源库列表页已统一。文章与资源详情阅读页会继续统一。为避免 Supabase 网络波动拖慢页面，公开内容请求设置为约 3.5 秒超时并降级为空状态。
5. 待进行：按已确认的轻量微交互统一按钮、卡片、搜索、筛选和弹窗。

本阶段已通过 `npm run typecheck`、`npm run lint`、`npm run build`。构建时 Supabase 临时网络不可达仍会打印资源降级提示，但构建成功；这不会启动或长期保留开发服务器。

### Phase C：上线前质量

1. 完善文章/资源 Metadata、OpenGraph、结构化数据、canonical、RSS、Sitemap。
2. 做响应式、键盘导航、焦点态、色彩对比、减少动画、404/空状态/错误状态检查。
3. 添加投稿防滥用和服务端限流策略。
4. 评估面向中国用户的部署、域名、邮件与可访问性方案。

## 7. 交接规则

- 优先复用当前代码，不要为展示编码能力从零重写。
- 改动前先读相关文件，避免无差别扫描 `node_modules`、`.next`、锁文件和生成目录。
- 不泄露 `.env.local`、Supabase URL/Key、管理员邮箱、Token 或用户隐私。
- 不删除旧模块或大范围重置 Git，除非项目 Owner 明确授权；优先隐藏、重定向或逐步替换。
- 每组实质修改后至少运行：`npm run typecheck`、`npm run lint`；发布前再运行 `npm run build`。
- `npm run build` 曾因 Supabase 网络临时不可达打印 `Unable to load resources: fetch failed`，但构建可成功完成并使用空数据降级。应改善网络/缓存策略，不要把它误判为编译失败。
- 不在用户不需要预览时启动并长期保持开发服务器。
